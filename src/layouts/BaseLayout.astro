---
import "../styles/global.css";
import Nav from "../components/Nav.astro";
import BottomNav from "../components/BottomNav.astro";
import { ClientRouter } from "astro:transitions";

interface Props {
  title?: string;
  activeTab: string;
}

const { title = "的士筆試・備考手冊", activeTab } = Astro.props;
---

<!doctype html>
<html lang="zh-HK">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="theme-color" content="#0f1013" />
    <meta name="description" content="的士筆試備考手冊——整合運輸署《的士營運小冊子》與《道路使用者守則》的溫習工具。" />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/manifest.webmanifest" />

    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="的士筆試" />

    <title>{title}</title>
    <ClientRouter />
  </head>
  <body>
    <div class="flex min-h-dvh flex-col">
      <header
        class="sticky top-0 z-20"
        style="background: color-mix(in srgb, var(--color-paper) 86%, transparent); backdrop-filter: saturate(1.2) blur(14px);"
      >
        <div class="mx-auto grid w-full max-w-(--container-content) grid-cols-[1fr_auto_1fr] items-center gap-6 px-8 py-4 max-tablet:px-5 max-tablet:py-3 max-sm:px-3">
          <a
            href="/"
            class="flex shrink-0 items-center gap-3 justify-self-start font-serif text-[22px] font-semibold tracking-[-0.01em] text-ink no-underline max-[520px]:gap-2.5 max-[520px]:text-[18px]"
            aria-label="返回首頁 · 的士筆試備考手冊"
          >
            <img
              src="/favicon.svg"
              alt=""
              width="30"
              height="30"
              class="size-[30px] rounded-lg max-[520px]:size-6"
              aria-hidden="true"
            />
            <span>的士筆試</span>
          </a>
          <Nav activeTab={activeTab} />
          <div aria-hidden="true"></div>
        </div>
      </header>

      <main
        class="mx-auto w-full max-w-(--container-content) flex-1 px-8 pt-14 pb-30 max-tablet:px-5 max-tablet:pt-10 max-tablet:pb-20 max-sm:pt-8 max-sm:pb-[calc(76px+env(safe-area-inset-bottom))]"
      >
        <slot />
      </main>

      <BottomNav activeTab={activeTab} />

      <footer
        class="mx-auto w-full max-w-(--container-content) border-t border-line px-8 pt-9 pb-12 text-xs leading-[1.7] text-muted max-tablet:px-5 max-tablet:pt-7 max-tablet:pb-10"
        aria-label="頁尾"
      >
        <div class="flex flex-wrap items-baseline justify-between gap-x-6 gap-y-2 border-b border-line pb-6 max-tablet:pb-5">
          <div class="flex items-baseline gap-2.5">
            <span class="inline-block size-2 translate-y-[1px] rounded-full bg-red" aria-hidden="true"></span>
            <span class="font-serif text-[15px] tracking-[-0.01em] text-ink-2">的士筆試備考手冊</span>
            <span class="text-[10.5px] tracking-[0.14em] text-ink-3 uppercase">2026 · 04</span>
          </div>
          <span class="text-[10.5px] tracking-[0.14em] text-ink-3 uppercase">第三方學習工具・非運輸署官方</span>
        </div>

        <div class="grid grid-cols-[1.4fr_1fr] gap-10 pt-6 max-tablet:grid-cols-1 max-tablet:gap-5 max-tablet:pt-5">
          <div class="flex flex-col gap-2.5">
            <div class="text-[10.5px] tracking-[0.14em] text-ink-3 uppercase">關於</div>
            <p>本應用整理自運輸署公開資料，僅供溫習用途。一切收費、規則、罰則以憲報及運輸署最新公布為準。</p>
            <p class="text-ink-3">純前端應用・不設帳戶・不收集個人資料・無 cookie 追蹤。</p>
          </div>

          <div class="flex flex-col gap-2.5">
            <div class="text-[10.5px] tracking-[0.14em] text-ink-3 uppercase">原文 PDF</div>
            <ul class="flex flex-col gap-2">
              <li>
                <a
                  href="https://www.td.gov.hk/filemanager/tc/publication/taxi%20operation%20booklet_tc_20260320_revised.pdf"
                  target="_blank"
                  rel="noopener"
                  class="text-ink-2 underline decoration-line-strong underline-offset-[3px] transition-colors hover:text-ink hover:decoration-red"
                >
                  的士營運小冊子（2026/03）
                </a>
              </li>
              <li>
                <a
                  href="https://www.td.gov.hk/filemanager/tc/content_172/road_users_code_2020_chi.pdf"
                  target="_blank"
                  rel="noopener"
                  class="text-ink-2 underline decoration-line-strong underline-offset-[3px] transition-colors hover:text-ink hover:decoration-red"
                >
                  道路使用者守則
                </a>
              </li>
            </ul>
          </div>
        </div>
      </footer>
    </div>

    <script is:inline>
      // Service worker: register on production hosts only. On any local host,
      // proactively unregister + purge caches in case a stale SW was registered
      // during a previous non-localhost session (e.g. 127.0.0.1 or a deploy).
      // Stale SWs serve outdated React-island bundles and cause "content flashes
      // empty" symptoms on client:load islands.
      (function () {
        if (!("serviceWorker" in navigator)) return;
        var host = location.hostname;
        var isLocal =
          host === "localhost" ||
          host === "127.0.0.1" ||
          host === "0.0.0.0" ||
          host.endsWith(".localhost");
        if (isLocal) {
          navigator.serviceWorker
            .getRegistrations()
            .then(function (regs) { regs.forEach(function (r) { r.unregister(); }); })
            .catch(function () {});
          if (window.caches && caches.keys) {
            caches.keys().then(function (keys) {
              keys.forEach(function (k) { caches.delete(k); });
            }).catch(function () {});
          }
          return;
        }
        window.addEventListener("load", function () {
          navigator.serviceWorker.register("/sw.js").catch(function () {});
        });
      })();
    </script>
  </body>
</html>
